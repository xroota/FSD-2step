mixin extra-list(data)
    .extra-list
      .extra-list__title= data.title
      each item in data.detailItems.items
        .extra-list__item
          .extra-list__item-pic= item.symbol
          .extra-list__item-text
            #{"strong"}=item.title
            #{"br"}= item.text