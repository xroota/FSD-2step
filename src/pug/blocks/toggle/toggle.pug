mixin toggle(data)
    .toggle(class=data.toggleClass)
        if data.title
          h3.toggle__title= data.title
        .toggle__item
          label.toggle__item-label
            input(class="toggle__item-input",type="checkbox", name=data.name, checked=data.checked)
            .toggle__item-title= data.text 